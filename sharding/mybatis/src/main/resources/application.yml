mybatis:
  type-aliases-package: com.gome.cn.sharding.mybatis.model
  mapper-locations: classpath:mapper/*.xml
mapper:
  mappers:
    - tk.mybatis.mapper.common.Mapper
  not-empty: false
  identity: MYSQL
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
