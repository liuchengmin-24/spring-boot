spring:
  profiles:
    active: test
  application:
    name: boot-redis

server:
  port: 8085
  context-path: /boot-redis
logging:
  exception-conversion-word: '%wEx'
  path: F:\tel\spring-boot\log
  level:
    root: info