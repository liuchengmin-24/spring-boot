spring:
  profiles:
    active: test
  application:
    name: boot-swagger

server:
  port: 8085
  context-path: /boot-swagger
logging:
  exception-conversion-word: '%wEx'
  path: F:\tel\spring-boot\log
  level:
    root: info